import React from 'react';
import Calculator from '../components/Calculator';

const RenderCal = () => (
  <div className="container">
    <p className="cal-p"> Lets do some math!</p>
    <Calculator />
  </div>
);

export default RenderCal;
